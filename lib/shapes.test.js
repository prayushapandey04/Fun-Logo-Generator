// Import

const {Circle, Square, Triangle} = require('./shapes');

// Testing code for Circle 

describe ('Shapes', () => {
    describe('Blue', () => {
        it ('should return an SVG logo as a blue shape that has rendered specific text in the middle, based on user input of shape, text color, and shape color')
    })
})